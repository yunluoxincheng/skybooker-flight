# 测试用例

说明：本文件是交付前测试执行清单。未实际执行前，“实际结果”统一写“未执行”，“状态”统一写“未执行”。后续执行时必须按真实结果更新。

## 1. 冒烟测试

| 编号 | 前置条件 | 测试步骤 | 期望结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|
| SMK-001 | MySQL、Redis 可用 | 启动后端 | 后端启动成功，Flyway 无失败 | `docker compose ps` 显示后端、MySQL、Redis healthy；后端 smoke 通过 | 通过 |
| SMK-002 | 后端可用 | 启动前端或完整 Compose | 首页可打开，无明显运行时错误 | `http://localhost:8088/` 返回 gateway 占位页，不是前端首页；已登记 Issue #86 | 未通过 |
| SMK-003 | 测试数据已导入 | 普通用户登录 | 登录成功，保存用户 token | UI 登录 `user1@example.com` 成功；smoke 用户登录返回 200 | 通过 |
| SMK-004 | 已登录 | 搜索广州到上海未来航班 | 返回航班列表，可进入详情 | `/flights` 返回航班列表，可进入未来航班详情/预订页 | 通过 |
| SMK-005 | 已登录且有可售座位 | 选择航班、乘机人、座位并提交订单 | 订单创建成功，状态为待支付 | 未来航班 `110124` + 座位 `10A` 创建订单成功；过期默认航班 `1` 提交失败，见 Issue #82 | 部分通过 |
| SMK-006 | 待支付订单存在 | 点击模拟支付 | 支付成功，订单状态为已出票 | 订单 `150121` 支付接口返回 200，状态变为 `ISSUED` | 通过 |
| SMK-007 | 已登录 | 打开我的订单和订单详情 | 只显示当前用户订单 | 已打开订单详情并成功读取当前用户订单；越权查看未执行 | 部分执行 |
| SMK-008 | 管理员账号可用 | 登录管理端 | 进入管理后台看板 | smoke 管理员登录和 dashboard summary 返回 200 | 通过 |
| SMK-009 | AI 接口可用 | 输入“明天广州到上海的便宜航班” | 返回真实航班推荐或明确追问 | smoke 对 `/api/ai/chat` 发送“我想去北京”返回 200；指定场景未执行 | 部分执行 |
| SMK-010 | 完整部署可用 | 运行 `scripts/smoke/backend-smoke.sh` | 脚本全部检查通过 | `SKYBOOKER_BASE_URL=http://localhost:8088 ... scripts/smoke/backend-smoke.sh` 通过 | 通过 |

## 2. 用户与鉴权

| 编号 | 前置条件 | 测试步骤 | 期望结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|
| AUTH-001 | 邮件模式已配置 | 发送注册验证码并注册新邮箱 | 注册成功，返回标准响应 | 未执行 | 未执行 |
| AUTH-002 | 邮箱已存在 | 使用相同邮箱再次注册 | 注册失败并提示“邮箱已注册” | 未执行 | 未执行 |
| AUTH-003 | 普通用户账号存在 | 用正确邮箱和密码登录 | 登录成功，返回 accessToken/refreshToken | `user1@example.com / User@123456` 登录成功并写入 token | 通过 |
| AUTH-004 | 普通用户账号存在 | 用错误密码登录 | 登录失败，不泄露敏感信息 | 未执行 | 未执行 |
| AUTH-005 | 管理员账号存在 | 在用户端登录管理员账号 | 拒绝登录并提示入口不匹配 | 未执行 | 未执行 |
| AUTH-006 | 管理员账号存在 | 在管理端登录管理员账号 | 登录成功并进入管理端 | smoke 使用 `admin / SkyBooker@Init2026!` 登录成功 | 通过 |
| AUTH-007 | token 缺失 | 访问 `/api/auth/me` | 返回未登录 | 未执行 | 未执行 |
| AUTH-008 | token 伪造或过期 | 访问受保护接口 | 返回 token 无效或已过期，前端清理登录态 | 未执行 | 未执行 |
| AUTH-009 | 打开登录页 | 邮箱填 `user1@example.com` 且密码留空；或邮箱留空且密码填测试值后点击登录 | 页面展示字段级错误，不发生原生 GET，密码不进入 URL，按钮恢复可点击 | 用户指定 `localhost:3000` 下通过，正常展示“请输入密码”或“请输入有效的邮箱地址”，URL 不带 query；`127.0.0.1:3001` 下失败并退回原生 GET，Issue #96 | 通过 |
| AUTH-010 | 打开注册页 | 空表单点击注册；或邮箱留空且密码/确认密码填测试值后点击注册 | 页面展示邮箱、验证码、昵称、密码等字段错误，不发生原生 GET，密码不进入 URL | 用户指定 `localhost:3000` 下通过，正常展示字段错误且 URL 不带 query；`127.0.0.1:3001` 下 URL 带出 `password/confirmPassword`，Issue #96 | 通过 |
| AUTH-011 | 打开找回密码页 | 空表单点击重置；或邮箱留空且新密码/确认密码填测试值后点击重置 | 页面展示邮箱、验证码、新密码等字段错误，不发生原生 GET，新密码不进入 URL | 用户指定 `localhost:3000` 下通过，正常展示字段错误且 URL 不带 query；`127.0.0.1:3001` 下 URL 带出 `newPassword/confirmPassword`，Issue #96 | 通过 |
| AUTH-012 | 打开管理端登录页 | 用户名填 `admin` 且密码留空；或用户名留空且密码填测试值后点击管理员登录 | 页面展示字段级错误，不发生原生 GET，密码不进入 URL | 用户指定 `localhost:3000` 下通过，正常展示“请输入密码”或“请输入管理员用户名”；`127.0.0.1:3001` 下 URL 带出 `username/password`，Issue #96 | 通过 |

## 3. 航班与座位

| 编号 | 前置条件 | 测试步骤 | 期望结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|
| FLT-001 | 已导入 `dev` 或 `test` seed | 按日期 + 航班号查询 | 返回匹配航班，分页字段正确 | 未执行 | 未执行 |
| FLT-002 | 已导入 seed | 按出发城市、到达城市、日期查询 | 返回同路线当天航班 | 未执行 | 未执行 |
| FLT-003 | 已导入 seed | 按价格、起飞时间、舱位、直飞筛选 | 结果符合筛选条件 | 未执行 | 未执行 |
| FLT-004 | 已导入 seed | 查询航班详情 | 展示航司、机场、城市、起降时间、价格、状态 | 已打开 `110124`、`1`、`110007` 的详情页，详情页日期展示正常；列表卡片缺少日期，Issue #87 | 部分通过 |
| FLT-005 | 已导入 seed | 查询座位图 | `AVAILABLE`、`LOCKED`、`SOLD`、`DISABLED` 显示正确 | 未执行 | 未执行 |
| FLT-006 | 存在售罄舱位 | 进入预订页选择舱位 | 售罄舱位不可误导为可订 | 未执行 | 未执行 |
| FLT-007 | 存在未发布/取消/已起飞航班 | 尝试进入详情或提交订单 | 不允许预订，并提示明确原因 | 过期航班 `1` 仍可进入预订并选座，提交后才返回 400“航班不可预订”；Issue #82 | 未通过 |

## 4. 乘机人

| 编号 | 前置条件 | 测试步骤 | 期望结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|
| PAX-001 | 已登录 | 新增成人乘机人 | 新增成功，列表刷新 | 未执行 | 未执行 |
| PAX-002 | 已登录 | 新增儿童、婴儿乘机人 | 类型展示为中文且一致 | 新增乘机人类型 trigger 显示 `ADULT`，下拉项为中文；Issue #80 | 未通过 |
| PAX-003 | 已登录 | 输入非法手机号或证件号并提交 | 前端展示具体校验错误，按钮恢复可点击 | 用户指定 `localhost:3000` 下，`/booking/210005` 新增乘机人空表单正常展示姓名、身份证号、手机号错误，按钮恢复为“确认添加” | 通过 |
| PAX-004 | 已登录 | 删除已有订单绑定的乘机人 | 删除失败并提示不能删除 | 未执行 | 未执行 |
| PAX-005 | 两个用户各有乘机人 | 使用 A 用户 token 修改 B 用户乘机人 | 返回无权限或资源不存在 | 未执行 | 未执行 |

## 5. 订单与支付

| 编号 | 前置条件 | 测试步骤 | 期望结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|
| ORD-001 | 可售航班、乘机人、座位存在 | 提交创建订单 | 创建成功，状态为 `PENDING_PAYMENT`，座位变为 `LOCKED` | 未来航班 `110124` 创建订单 `150121` 成功，状态 `PENDING_PAYMENT`；座位锁数据库未复核 | 部分通过 |
| ORD-002 | 可售航班存在 | 提交重复乘机人 | 创建失败，提示重复乘机人 | 未执行 | 未执行 |
| ORD-003 | 可售航班存在 | 提交重复座位 | 创建失败，提示重复座位 | 未执行 | 未执行 |
| ORD-004 | 已创建待支付订单 | 查询订单列表和详情 | 只返回当前用户订单 | 未执行 | 未执行 |
| ORD-005 | 待支付订单存在 | 取消订单 | 状态变为 `CANCELLED`，座位释放，余票恢复 | 未执行 | 未执行 |
| PAY-001 | 待支付订单存在 | 模拟支付 | 状态变为 `ISSUED`，座位变为 `SOLD` | 订单 `150121` 支付成功，状态变为 `ISSUED`；座位状态数据库未复核 | 部分通过 |
| PAY-002 | 已支付订单存在 | 再次点击支付 | 不重复扣减余票，不生成重复售票 | 未执行 | 未执行 |
| PAY-003 | 过期待支付订单存在 | 点击支付 | 支付失败，提示订单已过期 | 未执行 | 未执行 |
| PAY-004 | 订单归属 A 用户 | B 用户尝试支付或取消 | 返回无权限或资源不存在 | 未执行 | 未执行 |

## 6. 退票、改签与候补

| 编号 | 前置条件 | 测试步骤 | 期望结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|
| REF-001 | 已出票订单存在且距起飞超过 2 小时 | 提交退票 | 退票成功，订单变为 `REFUNDED`，座位释放 | 未执行 | 未执行 |
| REF-002 | 已出票订单距起飞不足 2 小时 | 提交退票 | 拒绝退票并提示窗口关闭 | 未执行 | 未执行 |
| CHG-001 | 已出票订单存在且同航线有候选航班 | 查询改签候选 | 返回可改签航班列表 | 未执行 | 未执行 |
| CHG-002 | 已出票订单距起飞不足 2 小时 | 查询或提交改签 | 拒绝改签并提示“距起飞不足 2 小时” | 未执行 | 未执行 |
| CHG-003 | 已出票订单、候选航班、可用座位存在 | 提交改签 | 订单航班、座位、金额、改签记录一致 | 未执行 | 未执行 |
| WLT-001 | 目标舱位无票 | 提交候补 | 生成待支付候补单 | 候补入口历史缺陷见 #88；用户指定 `localhost:3000` 下直接打开 `/waitlist/create?flightId=210007&cabinClass=ECONOMY`，新增乘机人空表单校验通过，完整提交候补未执行 | 部分通过 |
| WLT-002 | 候补单待支付 | 支付候补单 | 状态变为 `WAITING` | 未执行 | 未执行 |
| WLT-003 | 已支付候补排队中 | 退票释放同舱位座位 | 候补按顺序兑现并生成正式订单 | 未执行 | 未执行 |
| WLT-004 | 候补单存在 | 取消候补 | 状态和退款记录正确 | 未执行 | 未执行 |

## 7. AI 助手

| 编号 | 前置条件 | 测试步骤 | 期望结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|
| AI-001 | seed 已导入 | 输入“你好” | 返回旅行助手普通回复，不展示缺失字段 UI | 未执行 | 未执行 |
| AI-002 | seed 已导入 | 输入“北京有什么好玩” | 返回旅行建议，不展示航班库存或预订链接 | 未执行 | 未执行 |
| AI-003 | seed 已导入 | 输入“查广州到上海机票” | 追问出发日期 | 未执行 | 未执行 |
| AI-004 | seed 已导入 | 输入“明天广州到上海的便宜航班” | 返回来自数据库的航班推荐卡片 | 未执行 | 未执行 |
| AI-005 | 多轮会话 | 先查航班，再输入“改成早上出发” | 上下文更新，只变更时间偏好 | 未执行 | 未执行 |
| AI-006 | 模拟 AI 异常 | 触发后端降级或异常 | 前端给出可理解提示，不白屏不卡住 | 未执行 | 未执行 |

## 8. 管理端、部署与并发

| 编号 | 前置条件 | 测试步骤 | 期望结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|
| ADM-001 | 管理员已登录 | 打开看板 | 统计数据加载成功 | 未执行 | 未执行 |
| ADM-002 | 管理员已登录 | 管理航班舱位并生成座位 | 舱位、座位和价格一致 | 未执行 | 未执行 |
| ADM-003 | 管理员已登录 | 禁用/启用普通用户 | 普通用户状态变更，管理员账号不可被该接口操作 | 未执行 | 未执行 |
| ADM-004 | 普通用户 token | 请求 `/api/admin/**` | 拒绝访问 | smoke 中用户 token 请求 `/api/admin/me` 返回 403 | 通过 |
| ADM-005 | 管理员已登录 | 管理航司和机场基础资料 | 可查看、新增、编辑航司和机场，新增航班可选择航司/机场 | 后台无航司/机场管理菜单；`/api/admin/airlines` 和 `/api/admin/airports` 返回 404；后端 #90，前端 #91 | 未通过 |
| DEP-001 | 空库环境 | 启动后端 | Flyway 初始化成功 | 未执行 | 未执行 |
| DEP-002 | 完整 Compose 环境 | 访问 `/healthz` 和 `/api/flights` | nginx 转发正常 | `http://localhost:8088/api/flights` smoke 返回 200；前端首页未提供，见 Issue #86 | 部分通过 |
| CON-001 | JMeter、测试数据和可售座位已准备 | 运行同座位并发下单测试 | 只有一个请求成功，数据库只有一个订单绑定目标座位 | 未执行 | 未执行 |
