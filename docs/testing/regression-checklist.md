# 回归测试清单

本清单用于每次缺陷修复后快速确认主流程没有被破坏。未执行前状态均为“未执行”。

## 1. 主流程回归

| 编号 | 回归项 | 触发时机 | 实际结果 | 状态 |
|---|---|---|---|---|
| REG-001 | 用户登录、获取当前用户、退出登录 | 任意认证或请求封装变更后 | 未执行 | 未执行 |
| REG-002 | 首页搜索入口跳转到航班列表 | 首页、搜索组件、路由变更后 | 未执行 | 未执行 |
| REG-003 | 航班搜索、筛选、排序、详情 | 航班接口或前端列表变更后 | 未执行 | 未执行 |
| REG-004 | 新增乘机人、选择乘机人、选择座位 | 乘机人、预订页、表单校验变更后 | 未执行 | 未执行 |
| REG-005 | 创建订单、支付、查看订单详情 | 订单、座位、支付、舱位变更后 | 未执行 | 未执行 |
| REG-006 | 取消待支付订单并释放座位 | 订单状态机或座位逻辑变更后 | 未执行 | 未执行 |
| REG-007 | 退票后座位释放和候补兑现 | 退票、候补、座位逻辑变更后 | 未执行 | 未执行 |
| REG-008 | 改签候选、提交改签、座位和金额一致 | 改签、订单详情、座位逻辑变更后 | 未执行 | 未执行 |
| REG-009 | AI 助手普通聊天、追问、航班推荐、多轮上下文 | AI 接口或前端聊天变更后 | 未执行 | 未执行 |
| REG-010 | 管理端登录、看板、航班、订单、用户、AI 配置 | 管理端接口或布局变更后 | 未执行 | 未执行 |

## 2. 高风险专项回归

| 编号 | 回归项 | 关注点 | 实际结果 | 状态 |
|---|---|---|---|---|
| RISK-001 | `POST /api/orders` 创建订单 | 不再出现错误的“航班不可预订”；余票、座位和舱位一致 | 未执行 | 未执行 |
| RISK-002 | 表单校验错误展示 | Zod/后端校验错误可见，提交按钮不会永久 loading，表单字段不进入 URL | 已抽样执行登录页空密码/空邮箱；字段错误不展示，部分场景发生原生 GET，Issue #96。其它表单仍需矩阵回归 | 未通过 |
| RISK-003 | 角色访问边界 | 未登录、普通用户、管理员权限符合设计 | 未执行 | 未执行 |
| RISK-004 | 同座位并发下单 | 不能超卖，只有一个订单成功绑定座位 | 未执行 | 未执行 |
| RISK-005 | 支付/取消/退票/改签并发 | 状态机不能产生重复推进或余票错乱 | 未执行 | 未执行 |
| RISK-006 | 测试数据一致性 | `docs/17_TEST_DATA_GUIDE.md` 的一致性 SQL 返回空结果 | 未执行 | 未执行 |

## 3. 命令回归

| 编号 | 命令 | 预期 | 实际结果 | 状态 |
|---|---|---|---|---|
| CMD-001 | `cd backend && mvn test` | 后端测试通过 | 未执行 | 未执行 |
| CMD-002 | `cd frontend && pnpm lint` | 前端 lint 通过 | 未执行 | 未执行 |
| CMD-003 | `cd frontend && pnpm build` | 前端构建通过 | 未执行 | 未执行 |
| CMD-004 | `SKYBOOKER_BASE_URL=http://localhost:8088 scripts/smoke/backend-smoke.sh` | smoke 通过 | 未执行 | 未执行 |
| CMD-005 | `python3 scripts/generate_test_data.py --profile dev --seed 20260707 --base-date 2026-07-07` | 生成本地 `seed-dev.sql` | 未执行 | 未执行 |
| CMD-006 | `python3 scripts/validate_test_data.py --file backend/src/main/resources/db/seed/seed-dev.sql` | 静态校验通过 | 未执行 | 未执行 |

## 4. 缺陷修复回归要求

- 修复 S0/S1 缺陷后，必须执行主流程回归和对应专项回归。
- 修复 UI/UX 缺陷后，必须验证不破坏登录、搜索、下单、支付、订单查看主流程。
- 修复后端接口后，必须验证前端调用、接口错误提示和权限边界。
- 修复 AI 助手后，必须验证普通航班搜索流程不受影响。
