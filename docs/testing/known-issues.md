# 已知缺陷清单

当前清单来自用户交付前测试准备阶段反馈。2026-07-07 已完成 KI-001 至 KI-009 的浏览器与接口复现抽样，并已为确认缺陷创建 GitHub Issue。未覆盖到的同类页面仍需后续扩展验证，不得在未复测前标记为已修复或通过。

## 严重级别

| 级别 | 标准 |
|---|---|
| S0 阻塞 | 系统无法启动、主流程完全不可用、无法登录、无法下单、严重数据损坏 |
| S1 严重 | 核心功能错误、安全越权、支付/订单状态错误、用户数据泄露 |
| S2 一般 | 非核心功能异常、部分筛选错误、提示不准确、普通页面交互问题 |
| S3 轻微 | 样式细节、文案、间距、图标、轻微体验问题 |

## KI-001：首页登录后仍显示“免费注册”文案

| 字段 | 内容 |
|---|---|
| 问题编号 | KI-001 |
| 问题描述 | 用户登录后，首页底部“准备好开始你的旅程了吗？”下方仍显示“免费注册”字样。 |
| 复现步骤 | 1. 使用普通用户登录；2. 返回首页；3. 滚动到底部 CTA 区域；4. 观察按钮或文案。 |
| 预期结果 | 登录态下不再展示“免费注册”，应展示进入搜索、查看订单或继续旅程等登录态操作。 |
| 实际结果 | 已复现。普通用户 `user1@example.com` 登录后，Header 已显示用户头像 `S`，首页底部 CTA 仍显示“立即注册，体验 AI 智能购票的便捷”和“免费注册”。 |
| 影响范围 | 首页登录态 UI、用户体验、转化文案。 |
| 严重级别 | S3 |
| 当前状态 | 已复现 |
| 建议优先级 | P3 |
| 后续验证方式 | 使用已登录普通用户截图对比登录前后首页底部 CTA；检查刷新后状态是否一致。 |
| 关联 Issue | https://github.com/yunluoxincheng/skybooker-flight/issues/79 |

## KI-002：新增乘机人类型选项中英文不一致

| 字段 | 内容 |
|---|---|
| 问题编号 | KI-002 |
| 问题描述 | 新增乘机人界面中，乘机人类型选项显示英文，但下拉列表显示中文，展示语言不统一。 |
| 复现步骤 | 1. 登录普通用户；2. 进入航班预订页；3. 打开新增乘机人弹窗；4. 查看乘机人类型默认显示和下拉选项。 |
| 预期结果 | 乘机人类型默认值、已选值和下拉选项均使用中文：成人、儿童、婴儿。 |
| 实际结果 | 已复现。在 `/booking/110124` 的新增乘机人弹窗中，乘机人类型 trigger 显示 `ADULT`；展开下拉后选项为“成人 / 儿童 / 婴儿”。 |
| 影响范围 | 预订页、新增乘机人表单、乘机人类型展示。 |
| 严重级别 | S3 |
| 当前状态 | 已复现 |
| 建议优先级 | P3 |
| 后续验证方式 | 分别选择成人、儿童、婴儿，确认 trigger、列表、提交后乘机人卡片均显示中文。 |
| 关联 Issue | https://github.com/yunluoxincheng/skybooker-flight/issues/80 |

## KI-003：全站表单校验错误不展示且提交状态异常

| 字段 | 内容 |
|---|---|
| 问题编号 | KI-003 |
| 问题描述 | 用户反馈多个前端表单填写错误时页面不提示具体错误、提交状态异常；#81 关闭后需要按全站表单矩阵回归。自动化复测发现该现象与访问 origin 有关：`127.0.0.1` 下复现，用户指定的 `localhost:3000` 环境下已抽样通过。 |
| 复现步骤 | A. 使用 `http://localhost:3000` 打开 `/login`、`/register`、`/forgot-password`、`/admin` 并提交缺失字段；B. 登录后打开 `/booking/210005`、`/waitlist/create?flightId=210007&cabinClass=ECONOMY` 的新增乘机人表单并空表单提交；C. 管理员打开 `/admin/flights` 新增航班并清空必填字段提交；D. 对比 `http://127.0.0.1:3001` dev origin 场景。 |
| 预期结果 | 页面展示字段级错误或全局错误；提交按钮在校验失败后立即恢复可点击；表单不得退回浏览器原生 GET；密码等敏感字段不得进入 URL、历史记录或日志。 |
| 实际结果 | 2026-07-07 按用户指定环境 `http://localhost:3000` 复测：登录、注册、找回密码、管理端登录均正常展示字段错误，URL 不带 query，密码可见性按钮可从 `password` 切到 `text`；`/booking/210005` 新增乘机人空表单正常展示 `请输入姓名`、`请输入有效身份证号`、`请输入有效手机号`，按钮恢复为“确认添加”；`/waitlist/create?flightId=210007&cabinClass=ECONOMY` 新增乘机人同样通过；`/admin/flights` 新增航班清空必填字段后正常展示航班号、航司 ID、机场 ID、时间、价格、座位数、行李额等错误。此前 `http://127.0.0.1:3001` 下失败，是 Next dev 阻止 `127.0.0.1` dev resource 导致客户端事件未接管。 |
| 影响范围 | 用户认证、注册/找回密码、乘机人、预订、订单、支付、候补、退改签、管理端编辑表单等多个交互页面。 |
| 严重级别 | S1 |
| 当前状态 | 用户指定 `localhost:3000` 环境抽样通过；#96 已更正为 `127.0.0.1` dev origin/客户端事件接管问题。若仍有页面复现，需补充具体页面、输入数据和 console/network 证据 |
| 建议优先级 | P1 |
| 后续验证方式 | 建立表单错误矩阵，逐页输入非法数据，记录字段提示、全局提示、按钮 loading 恢复、URL 是否泄露字段、控制台报错和接口响应。 |
| 关联 Issue | #81（已关闭，原新增乘机人范围）；#96（当前全站修复不完整跟踪） |

## KI-004：提交订单返回“航班不可预订”

| 字段 | 内容 |
|---|---|
| 问题编号 | KI-004 |
| 问题描述 | 选择好航班、乘机人和座位后点击提交订单，提示“航班不可预订”，控制台显示 `POST /api/orders` 返回 400。 |
| 复现步骤 | 1. 登录普通用户；2. 从默认航班列表选择航班 ID `1`（`MU5101`，2026-06-18 08:30）；3. 进入 `/booking/1`；4. 选择乘机人“张三”和经济舱座位 `1B`；5. 点击提交订单；6. 查看页面提示和网络请求。 |
| 预期结果 | 订单创建成功，状态为 `PENDING_PAYMENT`，座位变为 `LOCKED`，可继续模拟支付。 |
| 实际结果 | 已复现但范围已澄清。过期航班 ID `1` 仍在默认列表中展示且可进入完整预订流程，提交时 `POST /api/orders` 返回 HTTP 400，响应为 `{"code":30001,"message":"航班不可预订","data":null}`。使用未来航班 ID `110124` 同流程下单成功，返回 200，订单状态为 `PENDING_PAYMENT`。 |
| 影响范围 | 核心下单流程、支付流程、座位库存、演示交付主链路。 |
| 严重级别 | S0 |
| 当前状态 | 已复现；问题集中在过期/不可订航班仍被默认展示为可预订 |
| 建议优先级 | P0 |
| 后续验证方式 | 使用 `docs/17_TEST_DATA_GUIDE.md` 生成的新 seed 复现；同时检查 `flight.remaining_seats`、`flight_seat.status`、舱位余票、航班 `publish_status`、`status` 和 `departure_time`。 |
| 关联 Issue | https://github.com/yunluoxincheng/skybooker-flight/issues/82 |

## KI-005：密码可见性按钮存在但点击无效

| 字段 | 内容 |
|---|---|
| 问题编号 | KI-005 |
| 问题描述 | 登录页面密码输入框无法切换明文/密文可见性；#83 关闭后按钮已出现，需确认不同访问 origin 下是否可用。 |
| 复现步骤 | 1. 打开 `/login`、`/register`、`/forgot-password`、`/admin`；2. 输入密码；3. 点击密码可见性切换按钮；4. 观察 input type 是否切换。 |
| 预期结果 | 密码框提供可见性切换，点击后在 `password` 和 `text` 类型之间切换，并有可访问标签。 |
| 实际结果 | 2026-07-07 回归验证：用户指定 `http://localhost:3000` 下，`/login`、`/register`、`/forgot-password`、`/admin` 均可从 `password` 切到 `text`；`http://127.0.0.1:3001` 下点击无效，归因于 #96 的 dev origin 问题。 |
| 影响范围 | 登录、注册、找回密码、管理端登录等密码输入体验。 |
| 严重级别 | S3 |
| 当前状态 | #83 在用户指定 `localhost:3000` 下通过；`127.0.0.1` 下随 #96 的 dev origin 问题复现 |
| 建议优先级 | P3 |
| 后续验证方式 | 修复 #96 后，逐页点击密码可见性按钮，确认 input type 在 `password` 和 `text` 之间切换且不影响表单校验。 |
| 关联 Issue | #83（已关闭）；#96（当前客户端事件回归） |

## KI-006：改签不可用或规则提示不清

| 字段 | 内容 |
|---|---|
| 问题编号 | KI-006 |
| 问题描述 | 改签功能不可用，或由于“起飞前 2 小时不可改签”等规则导致失败但前端没有明确提示。 |
| 复现步骤 | 1. 使用已出票订单；2. 打开订单详情；3. 点击改签或查询改签候选；4. 分别使用距起飞不足 2 小时和超过 24 小时的订单测试；5. 观察页面提示。 |
| 预期结果 | 可改签订单展示候选航班并可提交；不可改签订单明确提示原因，例如“距起飞不足 2 小时不可改签”。 |
| 实际结果 | 已复现。未来航班订单 `150121` 支付后状态变为 `ISSUED`，订单详情页展示“改签”按钮，但按钮固定 `disabled=true`，title 为“改签功能即将上线”；无法判断是功能未实现还是业务规则限制。 |
| 影响范围 | 订单详情、改签接口、改签规则提示、售后流程体验。 |
| 严重级别 | S2 |
| 当前状态 | 已复现；后端改签接口规则仍待专项验证 |
| 建议优先级 | P2 |
| 后续验证方式 | 使用 generated seed 中临近起飞和非临近起飞订单分别验证 `GET /api/orders/{id}/change-options` 与 `POST /api/orders/{id}/change`。 |
| 关联 Issue | https://github.com/yunluoxincheng/skybooker-flight/issues/84 |

## KI-007：航班卡片未显示日期

| 字段 | 内容 |
|---|---|
| 问题编号 | KI-007 |
| 问题描述 | 航班列表卡片只显示出发时间、到达时间和预计用时，未显示出发/到达日期。 |
| 复现步骤 | 1. 打开 `/flights`；2. 查看任意航班卡片；3. 对比 `/flights/{id}` 详情页日期展示。 |
| 预期结果 | 航班卡片应展示出发日期；跨天到达时应展示到达日期或 `+1天` 提示。 |
| 实际结果 | 已复现。`/flights` 首张卡片展示“中国东方航空 / MU5101 / 08:30 / 上海 / 2时15分 / 直飞 / 10:45 / 北京”，没有日期。代码层面 `FlightCard` 和 `AiFlightCard` 均只格式化 `HH:mm`。 |
| 影响范围 | 航班列表页、AI 推荐航班卡片、用户选航班体验；也会放大过期航班仍展示的问题。 |
| 严重级别 | S2 |
| 当前状态 | 已复现 |
| 建议优先级 | P2 |
| 后续验证方式 | 列表卡片展示出发日期；跨天航班显示到达日期或 `+1天`；AI 航班卡片同步展示日期。 |
| 关联 Issue | https://github.com/yunluoxincheng/skybooker-flight/issues/87 |

## KI-008：售罄航班缺少候补入口

| 字段 | 内容 |
|---|---|
| 问题编号 | KI-008 |
| 问题描述 | 前端没有可见候补模块；航班售罄后只显示“已售罄”，用户无法提交候补订单。 |
| 复现步骤 | 1. 打开售罄航班 `/flights/110007`；2. 查看舱位与操作区域；3. 访问 `/waitlist`；4. 检查导航和路由是否存在候补入口。 |
| 预期结果 | 航班/舱位售罄时可进入候补下单；用户可查看我的候补、候补详情，并完成候补支付/取消。 |
| 实际结果 | 已复现。`KE8501` 显示“总座位 22 · 可售 0”，经济舱和商务舱均“已售罄”，无“加入候补”入口；`/waitlist` 返回 404。前端存在 `waitlistApi.ts`，后端存在 `WaitlistController`，但用户端没有候补页面和售罄入口。 |
| 影响范围 | 候补购票主功能、售罄航班转化路径、订单/候补状态机验收。 |
| 严重级别 | S1 |
| 当前状态 | 已复现 |
| 建议优先级 | P1 |
| 后续验证方式 | 售罄航班详情页提供候补入口；“我的候补”页面可查看候补单；候补提交、支付、取消和退票释放后的兑现流程可从前端完成。 |
| 关联 Issue | https://github.com/yunluoxincheng/skybooker-flight/issues/88 |

## KI-009：后台缺少航司和机场管理（已拆分责任）

| 字段 | 内容 |
|---|---|
| 问题编号 | KI-009 |
| 问题描述 | 管理端没有航司和机场管理页面；新增/编辑航班只能手动输入航司 ID 和机场 ID；后端管理接口也未提供。原混合 Issue #89 已关闭，拆分为后端 #90 和前端 #91。 |
| 复现步骤 | 1. 管理员登录后台；2. 查看后台侧边栏；3. 打开 `/admin/flights` 并点击“新增航班”；4. 请求 `GET /api/admin/airlines` 和 `GET /api/admin/airports`。 |
| 预期结果 | 管理后台提供航司/机场管理页面；新增航班时可选择航司和机场；后端提供管理端航司/机场 CRUD 或至少列表接口。 |
| 实际结果 | 已复现。后台侧边栏只有数据看板、航班管理、订单管理、用户管理、AI 配置；新增航班弹窗只有 `航司ID`、`出发机场ID`、`到达机场ID` 输入框；`GET /api/admin/airlines` 和 `GET /api/admin/airports` 均返回 404 `资源不存在`。 |
| 影响范围 | 管理端基础资料维护、航班新增/编辑可用性、测试数据之外的真实运营能力。 |
| 严重级别 | S1 |
| 当前状态 | 已复现 |
| 建议优先级 | P1 |
| 职责拆分 | 后端：提供 `/api/admin/airlines` 和 `/api/admin/airports` 管理接口；前端：提供航司/机场管理页面，并在航班表单使用选择器。 |
| 后续验证方式 | 先验证后端 #90 接口可用和鉴权正确，再验证前端 #91 页面入口、列表维护、航班表单选择和编辑回显。 |
| 关联 Issue | 后端：https://github.com/yunluoxincheng/skybooker-flight/issues/90；前端：https://github.com/yunluoxincheng/skybooker-flight/issues/91；原混合项 #89 已关闭 |

## 历史审计待复核来源

历史审计文档中已有若干问题被后续 PR 修复或评估关闭。完整测试执行阶段应只把当前可复现问题登记为缺陷，历史项作为复核来源：

- `docs/audit/2026-06-29-frontend-backend-contract-review.md`
- `docs/audit/2026-06-30-issues-frontend.md`
- `docs/audit/2026-06-30-issues-backend.md`
- `docs/audit/2026-07-02-issues-frontend.md`
