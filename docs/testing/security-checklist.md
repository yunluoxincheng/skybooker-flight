# 安全测试清单

状态说明：未执行前状态保持“未执行”。安全测试不得使用真实生产账号、真实支付凭据或真实用户隐私数据。

## 1. 角色访问边界

| 编号 | 检查项 | 测试方式 | 期望结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|
| SEC-AUTH-001 | 未登录用户不能访问受保护接口 | 无 token 请求 `/api/auth/me`、`/api/orders`、`/api/passengers` | 返回未登录 | 未执行 | 未执行 |
| SEC-AUTH-002 | 普通用户不能访问管理端接口 | 用户 token 请求 `/api/admin/me`、`/api/admin/orders` | 返回无权限 | 未执行 | 未执行 |
| SEC-AUTH-003 | 普通用户不能查看他人订单 | A/B 用户订单交叉访问 | 返回资源不存在或无权限 | 未执行 | 未执行 |
| SEC-AUTH-004 | 普通用户不能修改他人订单 | A/B 用户支付、取消、退票、改签交叉操作 | 操作被拒绝 | 未执行 | 未执行 |
| SEC-AUTH-005 | 管理员权限范围符合系统设计 | 管理员访问管理端能力，验证普通用户管理保护 | 管理员只能执行设计允许的管理能力 | 未执行 | 未执行 |
| SEC-AUTH-006 | 订单、支付、改签、退票校验身份和资源归属 | 篡改 orderId、passengerId、seatId | 不允许越权或绑定他人资源 | 未执行 | 未执行 |

## 2. Token 与会话

| 编号 | 检查项 | 测试方式 | 期望结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|
| SEC-TOKEN-001 | token 缺失 | 删除 Authorization 访问受保护接口 | 返回未登录 | 未执行 | 未执行 |
| SEC-TOKEN-002 | token 伪造 | 构造非法 JWT | 返回 token 无效 | 未执行 | 未执行 |
| SEC-TOKEN-003 | token 过期 | 使用过期 token | 返回 token 过期，前端清理登录态 | 未执行 | 未执行 |
| SEC-TOKEN-004 | 用户端和管理端 token 隔离 | 用户 token 调管理端、管理 token 调用户端个人业务 | 符合 `docs/15_AUTH_DESIGN.md` | 未执行 | 未执行 |
| SEC-TOKEN-005 | 刷新 token 失效 | 使用无效 refresh token | 返回刷新令牌无效或过期 | 未执行 | 未执行 |

## 3. 输入与数据安全

| 编号 | 检查项 | 测试方式 | 期望结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|
| SEC-IN-001 | SQL 注入风险 | 搜索、排序、筛选传入注入 payload | 不执行注入，返回校验失败或安全结果 | 未执行 | 未执行 |
| SEC-IN-002 | XSS 风险 | 昵称、乘机人姓名、AI 输入、管理端表单输入脚本片段 | 页面转义显示，不执行脚本 | 未执行 | 未执行 |
| SEC-IN-003 | 参数篡改 | 篡改价格、座位、舱位、乘机人 ID | 后端以数据库数据为准并拒绝非法组合 | 未执行 | 未执行 |
| SEC-IN-004 | 错误堆栈暴露 | 触发 400/403/404/500 | 响应不暴露堆栈、SQL、密钥、服务器路径 | 未执行 | 未执行 |
| SEC-IN-005 | 敏感信息泄露 | 检查登录、用户、AI 配置响应 | 不返回密码哈希、明文 apiKey、验证码、JWT secret | 未执行 | 未执行 |

## 4. 业务安全

| 编号 | 检查项 | 测试方式 | 期望结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|
| SEC-BIZ-001 | 重复提交订单 | 快速重复点击提交订单或并发请求 | 不产生重复锁座或重复订单异常 | 未执行 | 未执行 |
| SEC-BIZ-002 | 重复支付 | 并发支付同一订单 | 不重复出票，不重复扣减余票 | 未执行 | 未执行 |
| SEC-BIZ-003 | 同座位并发下单 | JMeter 同座位并发测试 | 仅一个订单成功 | 未执行 | 未执行 |
| SEC-BIZ-004 | 候补兑现顺序 | 多个候补等待同一舱位 | 按规则兑现，不越权绑定 | 未执行 | 未执行 |
| SEC-BIZ-005 | AI 请求限流 | 高频匿名或同会话请求 | 达到限制时返回频率提示 | 未执行 | 未执行 |

## 5. 部署安全

| 编号 | 检查项 | 测试方式 | 期望结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|
| SEC-DEP-001 | `.env` 不提交 | 检查 Git 状态和 `.gitignore` | 无密钥文件进入 Git | 未执行 | 未执行 |
| SEC-DEP-002 | 默认管理员密码 | 检查 README 和部署说明 | 交付环境要求首次部署后修改默认密码 | 未执行 | 未执行 |
| SEC-DEP-003 | OpenAPI 暴露 | 检查生产环境配置 | 生产按配置关闭或受控暴露 | 未执行 | 未执行 |
| SEC-DEP-004 | AI provider key | 检查管理端 AI 配置响应 | apiKey 脱敏，不回显明文 | 未执行 | 未执行 |
