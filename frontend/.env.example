# SkyBooker Frontend Environment Configuration
#
# 本地开发：
#   cp .env.example .env.local
#   .env.local 按需修改，不提交到 Git
#
# 生产部署（同源 nginx 反向代理）：
#   cp .env.example .env.production
#   确保 NEXT_PUBLIC_API_BASE_URL=/api
#   构建时 Next.js 读取 .env.production
#
# 纯 IP / 独立端口部署（前后端不同源，需后端 CORS 放行）：
#   修改 NEXT_PUBLIC_API_BASE_URL 为后端完整地址，如 https://api.example.com/api

# 后端 API 基地址（供浏览器 fetch 调用）
# - 同源部署（nginx）：/api
# - 本地开发：http://localhost:8080/api
# - 跨域部署：https://api.example.com/api
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api
